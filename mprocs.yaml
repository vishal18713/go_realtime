procs:
  docker:
    shell: "docker compose up -d"
  backend:
    cwd: "backend"
    shell: "go run ./cmd/server"
  frontend:
    cwd: "frontend"
    shell: "npm run dev -- --host 0.0.0.0 --port 5173"
  admin:
    cwd: "admin-portal"
    shell: "npm run dev -- --host 0.0.0.0 --port 3000"
